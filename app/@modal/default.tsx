// @modal パラレルスロットの既定表示。
//
// モーダルが「アクティブでない」とき(= トップ等の通常表示、フルページ直アクセス、
// リロード時)はここが効いて何も描画しない。これにより
// /video/[id] への直アクセス・リロード・Google経由では従来どおりフルページのみが出る。
export default function Default() {
  return null;
}
